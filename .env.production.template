# =====================================================================
# PRASHANTH HOSPITALS — PRODUCTION ENVIRONMENT SECRETS TEMPLATE
# =====================================================================

# Application Production Identity
APP_NAME="Prashanth Hospitals Call Center & SLM Platform (Production)"
APP_ENV=production
DEBUG=false
SECRET_KEY=REPLACE_WITH_PRODUCTION_SECURE_64_CHAR_RANDOM_SECRET_KEY

# Operational Database Connection & Connection Pool Settings
DATABASE_URL=postgresql+psycopg2://ph_db_prod_user:REPLACE_WITH_PROD_DB_PASSWORD@10.0.1.50:5432/ph_callcenter_slm_prod_db
DB_POOL_SIZE=30
DB_MAX_OVERFLOW=50
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=1800

# XTEND DB2 Central Hub Replication Parameters (Kolathur Branch)
XTEND_DB2_HOST=10.0.1.100
XTEND_DB2_PORT=50000
XTEND_DB2_USER=xtend_prod_replicator
XTEND_DB2_PASSWORD=REPLACE_WITH_XTEND_DB2_PROD_PASSWORD
XTEND_DB2_DATABASE=XTENDDB2_PROD

# Firebase Cloud Messaging (FCM) Production API Tokens
FCM_SERVER_KEY=REPLACE_WITH_PRODUCTION_FCM_SERVER_KEY
FCM_PROJECT_ID=prashanth-hospitals-slm-prod

# Production CORS Allowed Origins
CORS_ORIGINS=https://callcenter-slm.prashanthhospitals.com,https://slm-api.prashanthhospitals.com
